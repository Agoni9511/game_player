spring:
  config:
    import: optional:classpath:application-private.yml,optional:file:./config/application-private.yml
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/peiwan_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:${app.local.db-password:}}
    driver-class-name: com.mysql.cj.jdbc.Driver
